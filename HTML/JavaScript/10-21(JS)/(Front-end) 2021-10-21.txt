2021-10-21
────────────────────────────────────────────────────────────────────────────────────
a {
display: block;
text-indent: -9999px;
// 들여쓰기, 내어쓰기 (a태그가 글자가 없으면 안되니까 아예 멀리 보내버린다.)
}

// 
e.preventDefault(); // 이것이 없으면 화면이동이 안된다.
// ↑ a태그에 이벤트(herf 속성)를 초기화를 시킨다.
 제이쿼리에서는 최대한 안쓰는게 좋다(a태그를 어쩔 수 없이 쓰는 경우 사용)
